#!/usr/bin/env node

var app = require('../app');

var config = require('../config/config');

app.listen(config.nodePort, function(){
	console.log('app  started, listening on port', config.nodePort);
});